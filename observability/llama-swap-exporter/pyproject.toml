[project]
name = "llama-swap-exporter"
version = "0.1.0"
description = "Prometheus Exporter for llama-swap."
readme = "README.md"
requires-python = ">=3.14"

dependencies = [
  "prometheus_client>=0.25.0",
  "python-dotenv>=1.2.2",
  "requests>=2.34.2",
]
